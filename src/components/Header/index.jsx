import styled from "styled-components";

const HeaderStyled = styled.header`
    padding: 60px 0;
    
`

const Header = () => {
    return (
        <HeaderStyled>
        </HeaderStyled>
    );
}

export default Header;